export interface CountriesTS {
  name: string;
  translations: {
    pt: string;
  };
  capital: string;
  population: number;
  region: string;
  numericCode: string;
  flags: {
    png: string;
  };
}
